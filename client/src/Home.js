import React from 'react';
const Home = () => {


  return (
   <div>
    <h1>This will create a navigation menu with links that are connected to the different routes in your app. When a user clicks on a link, the corresponding route will be displayed.

You can also customize the appearance of the  elements by using the activeClassName prop to specify a class that should be applied to the element when it is active. For example:    </h1>
   </div>
  );
};

export default Home;
